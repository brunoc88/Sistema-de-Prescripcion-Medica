doctype html
html(lang="es")
  head
    meta(charset="UTF-8")
    title Login Form
    link(rel="stylesheet", href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css")
  body
    .container
      .row.justify-content-center
        .col-md-6
          .card
            .card-body
              h2.card-title.text-center Iniciar Sesión
              form(action="/login", method="POST")
                .mb-3
                  label(for="email") Correo electrónico
                  input#email.form-control(type="email", name="email", placeholder="Introduce tu correo electrónico" required)
                .mb-3
                  label(for="password") Contraseña
                  input#password.form-control(type="password", name="password", placeholder="Introduce tu contraseña" required)
                .mb-3.text-center
                  button.btn.btn-primary(type="submit") Iniciar sesión
                .mb-3.text-center
                  a(href="/forgot-password") ¿Olvidaste tu contraseña?
                .mb-3.text-center
                  a(href="/register") No tienes cuenta? Regístrate
    script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js")
