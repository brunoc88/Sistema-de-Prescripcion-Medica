doctype html
html
  head
    title Detalle del Contrato
    link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css")
  body
    .container.mt-5
      h1.text-center Detalle del Contrato ##{contrato.idContrato}
      
      .card.mb-3
        .card-header.bg-primary.text-white
          h2 Contrato ##{contrato.idContrato}
        .card-body
          h5 Detalles del Contrato
          ul
            li Fecha de Creación: #{contrato.fechaCreacion}
            li Fecha de Caducidad: #{contrato.fechaCaducidad}
            li Estado: #{contrato.estado ? "Activo" : "Inactivo"}

          h5 Profesional Asignado
          ul
            li Nombre: #{contrato.Profesional.nombre} #{contrato.Profesional.apellido}
            li Domicilio: #{contrato.Profesional.domicilio}
            li Email: #{contrato.Profesional.email}
            li Profesión: #{contrato.Profesional.Profesion.nombre}
            li Especialidad: #{contrato.Profesional.Especialidad.nombre}

          h5 Usuario Responsable
          ul
            li Nombre: #{contrato.Usuario.nombre} #{contrato.Usuario.apellido}
            li Rol: #{contrato.Usuario.rol}
            li Email: #{contrato.Usuario.email}

      // Botón de volver
      .mt-4.text-center
        a.btn.btn-secondary(href="javascript:history.back()") Volver

      // Botón para descargar en PDF
      .mt-4.text-center
        a.btn.btn-danger(href=`/contrato/descargarPDF/${contrato.idContrato}`) Descargar en PDF
